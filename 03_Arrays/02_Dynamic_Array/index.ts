let strings2 = ["a", "b", "c", "d"];
// For 32-bit systems, 4*4 = 16 bytes are occupied


// 動的配列は要素数を増やす際、配列をコピーして移動し、その後新しく要素を追加している

// pushなどではO(1)となるが、たまにはn線形時間という状況もある



console.log(strings2); // ["x", "a", "apple", "b", "c"]
